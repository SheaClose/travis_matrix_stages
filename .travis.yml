dist: trusty
language: node_js
node_js: lts/*

jobs:
  include:
    - stage: first
      name: The first to run
      install: skip
      script:
        - ./first.sh
    - stage: Second
      script:
        - ./second1.sh
        - ./second2.sh
