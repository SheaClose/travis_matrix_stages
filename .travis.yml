dist: trusty
language: node_js
node_js: lts/*

jobs:
  include:
    - stage: first
      name: The first to run
      install: skip
      script:
        - ./first.sh
    - stage: Second B
      script:
        - ./second1.sh
      script:
        - ./second2.sh
